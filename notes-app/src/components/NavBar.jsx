import React from 'react';
import '../styles/NavBar.css';

const NavBar = function (){
    return(
        <nav>
            <div>
                MyNote.com
            </div>

            <div>
                <a>Login</a>
            </div>

        </nav>
    );
}

export default NavBar;